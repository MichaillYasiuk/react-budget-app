import styled from 'styled-components';

const StyledTitle = styled.h1`
  display: grid;
  grid-column-start: 1;
  align-items: center;
  padding: 0 20px;
  font-size: 24px;
`;

export { StyledTitle };
